import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#7ee8fa",
      dark: "#1d5c67",
      light: "#9fe6e6"
    },
    secondary: {
      main: "#097aa4"
    },
    background: {
      default: "#d7f3ff"
    }
  }
});
